#include<iostream> 
using namespace std; // for std::cout
int main()/* {
    cout << "nn\n\nnn\n" ; // cout is used to print the output
    cout <<"nn\n\nnn\n"; // we've cin for input from user
} */
{
    int a;
    if (3+2%5)
        cout << " this works\n";
    if (a=2)
        cout<< " this also works\n";
    if (0)
        cout <<" sorry this will not work\n";
    if (-8)
        cout << " omg this works too\n";
    
 // the point is not only condition but we can alse enter expressions in if conditions
}