#include<iostream>
using namespace std;
int main()
/* {
    char ch;
    cout <<"Enter a alphabet: ";
    cin >> ch;
  // a to z -> 97 to 122
  // A to Z -> 65 to 90
    int ascii = (int) ch;
    if ( (ascii>=97 && ascii<=122) || (ascii>=65 && ascii<=90))
    {cout<<"THE INPUT IS AN ALPHABET \n";}
        else {cout<<"The input is not an alphabet! \n";}

    if (ascii>=97 && ascii<=122){
        cout<<" And it is lowercase alphabet";}
    if(ascii>=65 && ascii<=90){
        cout <<" And it is uppercase alphabet";}
} */
 
