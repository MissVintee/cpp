#include <iostream>
using namespace std;
int main()

 /* {  // else if
    int n;
    cout << "Check the divisibility of a nubmer by 3, 5 & 15\n ";
    cout<<"Enter a number: ";
    cin>>n;
    int a, b, c;
    a = (n%3==0);
    b = (n%5==0);
    c = (n%15==0);
    if(a&&b&&c){ cout<<"divisible by 5, 3 and 15";}
    else if (a){ cout<<"divisible by 3";}
    else if (b){ cout << "divisible by 5";}
    else { cout<<"not matching condition";}
    
} */
{
    int k= 35;
    cout<<( k==35) << endl << (k=50) << endl << (k>40) << endl;
cout <<" here is some difference\n ";
    int l = 35;
    cout<<( l==35) << endl << (l==50) << endl << (l>40);
}
